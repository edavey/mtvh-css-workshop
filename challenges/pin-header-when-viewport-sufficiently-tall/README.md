# Challenge: pin header to top with viewport sufficiently tall

We want to 'pin' our header to the top of the viewport -- but only when the viewport is tall enough that the pinned header won't get in the way.

